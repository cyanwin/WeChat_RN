import React, { FC } from 'react'
import { View, Text } from 'react-native'

const Header: FC = () => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default Header
